# Telegram API
telethon>=1.34.0

# Database
pymongo>=4.6.0

# Web scraping (seeds)
scrapling>=0.2.0          # Playwright-powered stealth scraper
requests>=2.31.0          # HTTP fallback when Scrapling is unavailable

# Language detection
langdetect>=1.0.9         # Primary detector (Naive Bayes, 55 languages)
langid>=1.1.6             # Secondary/fallback detector

# NLP / Classification
transformers>=4.40.0      # RoBERTa fine-tuned model loading
torch>=2.2.0              # PyTorch backend (CPU or CUDA)
# Lightweight alternative (CPU-only inference without full PyTorch):
# onnxruntime>=1.17.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
